{{#if (int_eq week_no 1)}}
    <tr class="cal_week">
        {{!-- {{#for 1 7 1]}}
            <td>{{first_week this cal_data}}</td> 
        {{/for}} --}}
        {{!-- e.g. Dec 1st is a Friday. Use an offset. --}}
        <td>{{first_week 1 cal_data}}</td> 
        <td>{{first_week 2 cal_data}}</td> 
        <td>{{first_week 3 cal_data}}</td> 
        <td>{{first_week 4 cal_data}}</td> 
        <td>{{first_week 5 cal_data}}</td> 
        <td>{{first_week 6 cal_data}}</td> 
        <td>{{first_week 7 cal_data}}</td> 
    </tr> 
{{else}}
    {{#if (int_eq week_no 2)}}
    <tr class="cal_week">
        {{#each cal_data.weekday_range}}
            <td>{{second_week this @root.cal_data}}</td> 
        {{/each}}
    </tr>
    {{else}}
        {{#if (int_eq week_no 3)}}
        <tr class="cal_week">
            {{#each cal_data.weekday_range}}
                <td>{{third_week this @root.cal_data}}</td> 
            {{/each}}
        </tr>
        {{else}}
            {{#if (int_eq week_no 4)}}
            <tr class="cal_week">
                {{#each cal_data.weekday_range}}
                    <td>{{fourth_week this @root.cal_data}}</td> 
                {{/each}}
            </tr>
            {{else}}
                {{#if (int_eq week_no 4)}}
                <tr class="cal_week">
                    {{#each cal_data.weekday_range}}
                        <td>{{fourth_week this @root.cal_data}}</td> 
                    {{/each}}
                </tr>
                {{else}}
                    {{#if (int_eq week_no 5)}}
                    <tr class="cal_week">
                        {{#each cal_data.weekday_range}}
                            <td>{{fifth_week this @root.cal_data}}</td> 
                        {{/each}}
                    </tr>
                    {{else}}
                        <p>We got problems</p>
                    {{/if}}
                {{/if}}
            {{/if}}
        {{/if}}
    {{/if}}
{{/if}}